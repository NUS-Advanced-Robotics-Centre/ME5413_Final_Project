<launch>

  <!-- Connect the robot to a keyboard teleop controller -->
  <node name="teleop_twist_keyboard" pkg="teleop_twist_keyboard" type="teleop_twist_keyboard.py" output="screen" respawn="true"/>
  
  <!-- Launch GMapping -->
  <!-- <include file="$(find jackal_navigation)/launch/include/gmapping.launch" /> -->
  <include file="$(find me5413_world)/launch/include/gmapping_jackal.launch" />

  <!-- record the keyboard control command -->
  <!-- record the /gazebo/ground_truth/state and /odometry/filtered -->
  <node name="recordbag" pkg="rosbag" type="record" 
      args="/cmd_vel /gazebo/ground_truth/state /odometry/filtered -o /home/lee/Resources/control_cmd/" />

  <!-- Launch Rviz with our settings -->
  <node type="rviz" name="rviz" pkg="rviz" args="-d $(find me5413_world)/rviz/gmapping.rviz" output="log" respawn="true"/>
  
</launch>